package com.example.fotogram.utils.componentiUtils

import androidx.compose.runtime.Composable

//import com.example.fotogram.model.Adv

@Composable
fun AdCardWrapper( /*
    controller: CommunicationController,
    sessionManager: SessionManager*/
) {
   /* // 1. STATO: Qui salviamo l'annuncio appena scaricato
    var adData by remember { mutableStateOf<Adv?>(null) }
    val scope = rememberCoroutineScope()

    // 2. SCARICAMENTO: Appena appare a schermo, chiama il server
    LaunchedEffect(Unit) {
        val sid = sessionManager.getSid()
        if (sid != null) {
            adData = controller.getAdv(sid)
        }
    }

    // 3. VISUALIZZAZIONE: Se abbiamo i dati, mostriamo la card gialla
    if (adData != null) {
        PostCardAdv(
            adv = adData!!,
        )
        Spacer(modifier = Modifier.height(16.dp))
    }*/
}